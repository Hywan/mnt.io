<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
  <channel>
    <title>mnt.io
    {%- if term %} - {{ term.name }}
    {%- elif section.title %} - {{ section.title }}
    {%- endif -%}
    </title>
    <link>{{ config.base_url | escape_xml | safe }}</link>
    <description>{{ config.description }}</description>
    <language>{{ lang }}</language>
    <atom:link href="{{ feed_url | safe }}" rel="self" type="application/rss+xml"/>
    {% if last_updated is defined %}<lastBuildDate>{{ last_updated | date(format="%a, %d %b %Y %H:%M:%S %z") }}</lastBuildDate>{% endif %}
    {%- for page in pages | filter(attribute="draft", value=false) %}
    <item>
      <title>{{ page.title }}</title>
      <pubDate>{{ page.date | date(format="%a, %d %b %Y %H:%M:%S %z") }}</pubDate>
      <author>ivan@mnt.io (Ivan Enderlin)</author>
      <link>{{ page.permalink | escape_xml | safe }}</link>
      <guid>{{ page.permalink | escape_xml | safe }}</guid>
      {% if page.description -%}
      <description xml:base="{{ page.permalink | escape_xml | safe }}">{{ page.description | markdown(inline=true) | escape_xml | safe }}</description>
      {%- endif %}
    </item>
    {%- endfor %}
  </channel>
</rss>
